package validatecreditcardnumber;

public class Validate{
	public static boolean validate(String n){
		// TODO: solution
		int[] arr = new int[n.length()];
		for (int i = 0; i < n.length(); i++) {

		}
		for (int i = 0; i < chars.length -1  ; i++) {
			int curIndex = chars.length -1 -i;

			(int)chars[curIndex]
		}
	}
}